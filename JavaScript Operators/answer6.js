// Q.6 Write a Javascript program to calculate the Body Mass Index (BMI) using the formula BMI = weight (kg)/
// height * height.

let weight = 70;   // in kilograms
let height = 1.75; // in meters

let bmi = weight / (height * height);

console.log("Your BMI is: " + bmi);